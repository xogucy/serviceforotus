apiVersion: v1
kind: ConfigMap
metadata:
  name: users-service-config
data:
  APP_NAME: users-service
  APP_ENV: k8s
  APP_HOST: 0.0.0.0
  APP_PORT: "8000"
  POSTGRES_DB: users_service
  POSTGRES_HOST: users-db-postgresql
  POSTGRES_PORT: "5432"
  JWT_ALGORITHM: HS256
  ACCESS_TOKEN_EXPIRE_MINUTES: "30"
  LOG_LEVEL: INFO
